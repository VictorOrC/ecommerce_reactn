import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    marginTop: 16,
  },
  title: {
    color: "#000",
    fontSize: 16,
    fontWeight: "600",
    marginBottom: 8,
  },
  emptyText: {
    color: "#9ca3af",
    fontSize: 13,
  },
});

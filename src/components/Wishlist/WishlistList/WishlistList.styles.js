import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    paddingHorizontal: 16,
    paddingVertical: 12,
  },
  title: {
    fontWeight: "600",
    fontSize: 20,
    marginBottom: 8,
    color: "#0f172a",
  },
});
